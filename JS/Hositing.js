/*
 -declare all variable that declare inbetween at  top of scope
 -without any declaration scope of variable is global
- memory is allocate each nd every variable when program start to run
- es6  arrow function also declared as variable so before initialization if we used it give reference error
-let const before use give error before declaration
*/

console.log("value of " + x);// access
hello();//
var x = 7;
function hello(){

    // k = 7;
    console.log("value of " + i);
    // let i = 100;
    var i = 100;
}
